consume = caller consumes a packet, the TX path is done with the SKB
free = caller drops the packet.
Only semantic (different trace output) but the effect the same.
![[Pasted image 20240124170946.png]]